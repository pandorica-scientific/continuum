# v0.3.8 — Statement ingest

Branch `feat/v0.3.8-bank-import`. Written after probing sixteen real and synthetic
statements from nine institutions; the evidence behind every decision here is in
`scratch-workspace/probe/` and the findings report.

**Revised 2026-08-17** after three independent audits — two agents and an external
Codex review — measured this branch against the claims below. Roughly a third of them
were false. Every status line in this document is now backed by a probe or a line
reference, and the sections that were aspirational are marked as such rather than
described in the past tense.

**Updated 2026-08-18.** Phase C is largely landed. What follows records the state
before it; the summary immediately below records what changed and what the numbers
are now. `npm test` (1213 passing), `npx tsc --noEmit` and `npm run lint` are green.

### Phase C, as landed

| Item                                           | State                                                                                                                                                                                                                  |
| ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1 — one proof gate, no exceptions             | **done.** Adapters and standards parsers now run through `assertAgreesWithItsOwnNumbers`; an adapter that claims a file and finds nothing, or throws, falls through to the generic reader instead of ending the import |
| C2 — `verifiedProfile` below the proof check   | **done.** P0 is refused even for a confirmed layout                                                                                                                                                                    |
| C3 — the account is the authority for currency | **done.** `'CZK'` default deleted; `ParseOptions.currency` threaded to `readTabular`; symbol test made bidirectional; undetermined currency is a question                                                              |
| C4 — ledger amount in the account's currency   | **done.** A per-row currency now populates `originalCurrency`, never `currency`                                                                                                                                        |
| C5 — surface summary-filtered rows             | **done.** `droppedMovements` raises a question instead of deleting silently                                                                                                                                            |
| C6 — scope evidence to its own reading         | **done.** Alternative decodings no longer share evidence; PDF pages of one document still do                                                                                                                           |
| C8 — disagreement is a question                | **done.** Ranked by proof class, then coverage, refusing only when two equally complete readings contradict — and a workbook's sheets are now COLLECTED rather than chosen between, so a two-account file imports both |
| C10 — assert field values in the corpus        | **done.** Currency asserted per statement and per row                                                                                                                                                                  |
| C13 — `isDateLike` range check                 | **done**                                                                                                                                                                                                               |
| C7 — anchor P3                                 | **done**, in ingest rather than in the proof engine — see below                                                                                                                                                        |
| C9 — bound inflated bytes, add caps            | **done.** Entries are inflated under a shared byte budget, so a lying central directory no longer passes; sheet and cell caps added; the walk is time-bounded                                                          |
| C11 — land or delete unreachable code          | **OCR landed** — it works and is wired to the queue. The profile store stays marked, not deleted                                                                                                                       |
| C12 — `frompdf.ts:235`, `regions.ts:96`        | **done.** Both were real defects; fixing them moved no numbers, because the rhythm assembler already covers what they would have unlocked — see below                                                                  |
| C14 — rhythm assembler                         | **done** — see below                                                                                                                                                                                                   |

Two defects were found during the work that no audit had identified, and both were
larger than anything on the list:

- **The generic reader split CSV on newlines before parsing quotes.** A newline inside
  a quoted field — a wrapped payment note, a two-line merchant address — cut one
  movement into two rows carrying neither a date nor an amount. `csvLines` had solved
  this and the five adapters had always used it; `grid.ts` never did. Sixty files in
  the synthetic corpus fragmented, recovered a fraction of their rows, and were refused.
- **The amount/balance differences test was specified in Phase 4 and never built.**
  "If one column's consecutive differences equal another column's values, both are
  identified at once" is what makes a headerless ledger readable, and without it every
  headerless export was refused with "nothing in the statement could be checked" while
  its own last column proved every movement in it. Now implemented as
  `pairAmountWithBalance`, over both row orders and both decimal conventions.

Two fixture defects surfaced the moment arithmetic became mandatory, which is the
clearest evidence the gate works: `tests/fixtures/fio.csv` stated a debit total of
50 050 against rows totalling 40 050, and the `fioStatement` generator printed opening
and closing balances of 0,00 while emitting a real movement. Both are corrected.

One proof bug was fixed along the way: `testChain` reported an **untestable** chain as
a **failed** one. A single movement with a single balance and no opening figure is not
a broken chain, and calling it "the running balance does not follow from the movements"
said something false about a consistent file.

### Phase C item 14 — the rhythm assembler, as landed

`src/lib/server/import/tabular/rhythm.ts`. Both assemblers now produce candidate
readings for every PDF and the proof engine chooses between them, which is the design
the comparison study argued for.

Integration took five changes beyond the port itself, each found by measurement:

1. **Repeated values get their own column slot.** Prose wraps and values do not: joining
   a booking date and a value date in one column produced `09.12.2024 08.12.2024`, which
   is not a date, and a 28-record table read as three.
2. **Page furniture is kept.** The assembler drops every line that is not a movement, so
   emitting only movements produced tables with no arithmetic attached — four banks
   assembled perfectly and were refused with "nothing in the statement could be checked",
   the balances having been discarded one step earlier.
3. **Movements are emitted contiguously, furniture after.** A multi-page statement prints
   furniture BETWEEN pages, so interleaving cut one 28-movement table into regions of 7,
   12, 7 and 2.
4. **The column header is recovered by geometry.** It is page furniture by the
   assembler's rule, and without it a `Debit | Credit | Balance` layout has no names and
   the reader guesses one of the pair. Labels are not aligned the way their values are —
   `Debit` sits left of a right-aligned figure — so they are matched on cell midpoints,
   the one position both alignments share. A line carrying both a date and a figure is a
   movement and is never taken as a header.
5. **A short page is carried by the document's rhythm.** Three anchors establish a beat,
   so a final page holding one movement produced no run and contributed nothing: a
   nine-movement statement read as eight and reconciled against nothing. The beat is a
   property of the statement, and by then it is known.

Two further fixes came out of the same work:

- **Evidence is scoped to the assembler that produced it.** Both assemblers read the same
  furniture and extract it differently; sharing across them cost a five-movement
  statement its proof — P4 with every check passing on its own evidence, P0 with the
  other assembler's stated totals.
- **The decimal mark is resolved against the currency's own precision.** "Three trailing
  digits reads equally as a thousands group" is true of a two-decimal currency and false
  of a three-decimal one, so every KWD statement had an undecidable separator and no
  readable amount. The account states the currency, so this is known rather than guessed.

The recorded limitation is deleted from `frompdf.ts` and `corpus.test.ts`. It was wrong
in the way that matters most: it described a defect as a boundary.

### The safety boundary, as landed

The old boundary read the sizes a zip states about itself, which is a defence against
honesty. Measured: a 299 KB upload declaring **0 bytes** of content passed every check,
and SheetJS inflated it anyway — RSS moved 812 MB in 586 ms.

Now the declared sizes are still read, because they give the clearest message when they
are honest, and then every entry is actually inflated under a **shared** byte budget —
shared rather than per-entry, so the total work is bounded however the payload is
divided. Stored entries are charged to the budget without inflating, since their output
is their input and the input is already measured. The walk is time-bounded too.

```
honest 300 MB declaration          299 KB upload   REFUSED
declares 0 bytes, expands 300 MB   299 KB upload   REFUSED   <- previously ACCEPTED
declares 12 KB, expands 300 MB     299 KB upload   REFUSED   <- previously ACCEPTED
an honest small workbook                           ACCEPTED
```

Plus a sheet cap and a per-sheet cell cap, checked against the declared range before any
of it becomes values: bounding the compressed bytes bounds the XML, and the XML is not
what costs the memory. Pinned in `tests/unit/safety-zip.test.ts`.

### OCR — it works, and our own code was what stopped it

This was recorded twice as "has never read a statement": 15 refusals, then 64 after every
tabular and rhythm fix was in place. Both measurements were honest and both were taken
against the wrong thing.

Rendering the same source PDF ourselves, at the same 300 dpi, and recognising that gives
**every figure on the page exactly right**. So recognition was never the problem, and the
corpus raster fixtures are not as clean as their `clean-raster` label claims. Four
explanations had already been ruled out along the way — the language count, the `_fast`
models, the image dimensions, the missing DPI metadata — and none of them was it either.

With a good raster, two defects in `ocr.ts` were in the way, and neither fails loudly,
because in both cases the recognised **text was already perfect**:

1. **Words were handed on individually.** A PDF's text layer emits phrases — "Cash
   withdrawal / Vector Mobile" is one item — and every reader downstream clusters cells
   into columns by their edges. Five words became five columns, and one description tore
   the table apart.
2. **The page was upside down.** Tesseract reports pixel coordinates, where y grows
   downward; the readers were written for a text layer, where it grows upward. The footer
   became the first record, the movements came out reversed, and the column header — found
   by looking ABOVE the first movement — was looked for below it. Without a header the
   roles fall back to shape, and a `Debit | Credit` pair then reads as one amount column
   with half its rows empty.

Measured after both, on the synthetic corpus rendered at 300 dpi: **8 of 20 statements
read exactly, and every statement that is filed is exact.** Nothing is imported wrongly;
the rest are refused. That was the property that mattered even while this did not work.

Reachable at last, and only where it belongs: `ingestFile` takes an `ocr` option and only
the **queue** passes it. Seconds per page is fine in the background and never on a
request — which is precisely the dependency the plan recorded between Phase 6 and Phase 7,
now discharged in the right direction.

Pinned by `tests/unit/ocr.test.ts`, which asserts the two things that silently broke it:
that a description arrives as one cell, and that the header sits above the movements.

**The profile store is still unreachable** — `saveProfile` and its siblings have no
callers because the mapping wizard does not exist, so `import_profile` is always empty.
It is marked in its own header rather than deleted, for the reason given there.

### Phase 8 — the audit, and what it found

Every server entry point was swept: 21 page loads and their actions, 16 endpoints, one
layout load, and the boot hook. The question asked of each was narrow — does anything here
run for an unbounded time while someone is waiting?

**One thing did.**

`runBackupNow` awaited `runBackup()` inside a form action. That call dumps the entire
database to SQL, writes it, and copies every uploaded file — on a household with years of
statements and documents, not a moment. The browser held the request open for all of it.

Inspecting it surfaced a second defect nobody had asked about: **nothing stopped two
backups overlapping.** The hourly scheduler and a person pressing the button are
independent, and both write the same temporary file before renaming it into place, so the
loser of that race would have left a truncated file as the only backup. That was reachable
without anyone doing anything unusual.

Both are fixed together. `runBackup` is now single-flight — a second caller joins the run
already under way rather than starting another — and the action starts it without
awaiting, returning immediately. The outcome is not lost by returning early: it is
recorded under `backupLastRun`, which the page already reads. The button shows
"Backing up…" while it runs. Pinned in `tests/unit/backup.test.ts`.

**Everything else was already sound, and each is recorded here so the next sweep does not
have to rediscover it.**

| Where                                                                         | Why it does not block                                                                                                                                                        |
| ----------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `computeNetWorth()` on **every** page load                                    | Six parallel queries over household-sized tables and one rate table. Already optimised for exactly this — a comment in place records that it used to be a query per holding. |
| FX refresh, scheduled backups, meter sync, calendar sync, net-worth snapshots | All on ticks in `hooks.server.ts`, started with `void` and never awaited by a request.                                                                                       |
| Statement import                                                              | Queued as of Phase 7. Reading is the slowest thing this product does and no longer happens on a request at all.                                                              |
| `/ics/<token>` calendar feed                                                  | Bounded to ±365 days by construction.                                                                                                                                        |
| `/api/v1/transactions`                                                        | Paginated; splits and tags are loaded for the page's ids, not for the table.                                                                                                 |
| `/settings/export`                                                            | Configuration only.                                                                                                                                                          |
| Documents page                                                                | Eleven selects, but issued in parallel, no file I/O, no query-per-row. Bounded by household scale.                                                                           |
| Loans, property, retirement pages                                             | Unbounded selects over tables a household counts in single figures.                                                                                                          |
| Setup: password hashing in a loop                                             | Slow deliberately — that is what a password hash is for — and it runs once, for one to three people.                                                                         |

Two things to watch rather than fix now: the `document` table is selected without a limit
and is the only one of those that grows without bound over years; and the import page polls
every 1.5 seconds while the queue has work, which is three cheap counts per poll and stops
the moment the queue empties.

### OFX/QFX — done, and QIF deliberately not

OFX is the third published statement format worth writing once, and the one most likely
to be what a North American bank offers: Quicken and its descendants made QFX the default
export for a generation of institutions that never shipped CAMT or MT940.

Two dialects share the extension and both are read by one scanner. **OFX 1.x is SGML** —
tags are opened, never closed, and a value runs to the end of its line — so it is not
XML and never was; handing it to an XML parser fails with a message about malformed
markup that tells the person who exported it nothing. OFX 2.x is ordinary XML. The shape
they share is a tag followed by a value, and nothing here depends on nesting.

Three traps, all handled and all pinned in `tests/unit/ofx.test.ts`:

- **The sign comes from `TRNAMT`, never from `TRNTYPE`.** The type is descriptive —
  DEBIT, CREDIT, CHECK, FEE, INT — and banks disagree about which of them imply a
  direction. The amount does not disagree with itself.
- **`LEDGERBAL`, not `AVAILBAL`.** Available balance is what may be spent; a pending card
  authorisation makes them differ, and reading the wrong one puts the statement at odds
  with its own movements.
- **A transaction ends where the next begins**, with no closing tag to wait for, so it
  must be filed BEFORE the tag that ended it is processed. Doing it afterwards resets the
  fields first and files an empty row — which reads as a file containing exactly one
  movement however many it has. That is what the first run produced: 1 row from every
  file.

The opening balance is derived from the stated closing balance less the movements. That
cannot manufacture agreement — both sides come from the same movements — but it turns an
unavailable endpoint check into a real one.

**Result: 10 of 10 OFX files exact.**

**QIF is refused on purpose.** It records a date, an amount, a payee and a memo and
nothing else: no balances, no totals, no count, no currency. There is nothing in the file
for the arithmetic to check, so a QIF could only ever be taken on trust — the one thing
this reader does not do. A parser for it would always refuse, which is precisely the
"looks finished, does nothing" shape this release has been removing. It is now _detected_
so it can be turned away for the right reason, and the message names the formats the same
bank almost certainly also offers.

### Rejoining fields that split on the delimiter

The largest single gap was 30 files where the delimiter and the decimal mark are the same
character, so `799,56` is indistinguishable from two fields `799` and `56`.

**Is that format real?** Not demonstrably as a bank export, and it should be rare: comma-
decimal locales use semicolons precisely because of this ambiguity — Excel switches its
list separator to `;` in those locales — so a bank shipping comma-on-comma would produce
files Excel itself cannot open. But the _general_ defect is common and has nothing to do
with decimals: an unquoted comma inside a description splits a row exactly the same way.
So the repair is written for the general case.

**Can the balance decide it?** Yes, and that is the whole mechanism. The rejoined reading
is offered as one more CANDIDATE beside the unrepaired one, and the proof engine chooses
between them as it chooses between encodings and delimiters. A wrong join produces a chain
that does not close, and is refused like any other misreading.

Nothing tries to work out how many joins are needed. Guessing the table's width from the
rows is circular: on a file where every movement row has split, the split rows ARE the
majority and the width they agree on is the broken one. Every pair that could be a split
number is joined instead, and the arithmetic rules on it.

One limit, worth knowing because it decides when this is settleable at all: **dropping the
cents from an amount and from the balance beside it leaves a chain that still closes.**
`6127 − 425 = 5702` exactly as `6127.92 − 425.23 = 5702.69` does. What separates the
readings is a borrow across the decimal point — `6127.92 − 425.99 = 5701.93`, where the
truncated figures give 5702. Over a real statement that occurs within the first few
movements, which is why the repaired reading wins on proof class there; over a handful of
rows that happen not to borrow, both readings prove themselves and the file is refused as
genuinely undecided rather than guessed at. That case is pinned as a test.

**Result: the CSV gap fell from 30 files to 3**, and those three are dormant statements
with no movements and no balances — nothing in them to read and nothing to check.

Two smaller things came out of the same work. An adapter that claims a file, fails, and is
overruled no longer puts its own complaint in the error: "mBank: transaction header not
found" named a bank the person may have nothing to do with, on a file mBank never wrote.
And the provenance now records `delimited-rejoined`, so a repaired reading says so.

### C12 — the two geometry defects, and a correction to how this was recorded

This was first written down as "not needed as stated", on the strength of a `regions.ts`
rewrite that broke three tests and was reverted. That was the wrong conclusion from the
right action: reverting a change that breaks tests is correct, but recording the revert as
a finding turned an unfinished investigation into a verdict. Breaking tests is a reason to
understand the rule better, not a reason to stop.

Both defects were real.

**`frompdf.ts:235` — the absorption window never advanced.** A record's `y` stayed at its
FIRST line, so the test `|previous.y - line.y| <= pitch * 3` measured distance from the
record's start rather than from the line just absorbed. Every record was therefore capped
at three pitches however many lines it had — and the banks that keep hand-written parsers
print two to five per movement, so the tail of each one became a record of its own. Fixed
by advancing `y` as lines are taken in.

**`regions.ts:96` — a table could be split from its own header.** Splitting on exact
populated width is right for a spreadsheet and wrong for anything recovered from page
geometry, where a movement carrying no value date populates one column fewer than the one
above it. Regions now split on WHICH columns are filled rather than how many.

That change surfaced two things the first attempt had not understood:

- A header populates every column the table has, while the rows beneath populate only the
  ones they use — so the first data row often overlaps the header least, and the header
  ends up in a region of its own. The table then has no names, roles fall back to shape,
  and a `Paid out | Paid in` pair reads as one amount column with half its rows empty.
  **That is the same failure the OCR path had, for the same reason.** A lone row that
  looks like a header is now folded into the block beneath it.
- `findHeader` alone is too generous for a row standing on its own: `Balance carried
forward  £3,521.59` names a role and carries no date, so it reads as a header. A header
  is all labels, so a lone candidate carrying money is not one.

**One behaviour genuinely improved.** mBank prints its closing balance in a row the same
width as a movement, directly below them. It used to be caught inside the transaction
region and filtered out there — kept out of the ledger, and thrown away. It is now its own
summary region, read as the statement's own closing balance, which is something the
arithmetic can check the movements against. `excludedRows`, which existed to report the
discarded row and had no callers, is deleted.

**Neither corpus moved: 274 of 294, and 12 real files importing.** That is the honest
outcome — two latent defects closed, no new capability, because `rhythm.ts` already reads
the layouts they would have unlocked. Worth doing, and worth saying plainly rather than
implying a win.

### Phase 5 — the mapping wizard, as landed

A refusal is the right answer for a layout that cannot prove itself. On its own it is
also a dead end: the person knows what their bank's columns mean and had no way to say
so, because the reader threw away everything it had worked out along with the error.

Three pieces close that.

**`previewLayout`** returns the best candidate reading whether or not it proved itself —
headers, inferred roles, a few sample rows, and what stopped it. It decides nothing and
imports nothing.

**The wizard** asks one question per column, in the words a statement uses rather than the
words the parser uses: _Date_, _Amount_, _Money in_, _Money out_, _Balance after_.

**`confirmMapping`** saves the answer as a verified profile and reads the file with it.

The design point is what a confirmation does NOT buy. It answers "what are these
columns" — the one question a person is better placed to answer than the file. Whether
the movements add up is still decided by the balances, and a mapping that produces rows
contradicting them is refused exactly as an inferred reading is. `decideImport` enforces
this: a verified profile can carry a P1 or a P2 past the gate, and a P0 is refused however
confidently it was mapped. Both directions are pinned as tests.

Two things had to be fixed to make it work at all, and both were real gaps rather than
wiring:

- **`headersOf` returned nothing when no header was recognised** — which is precisely the
  case a profile exists for. A bank writing its labels in a language the dictionary does
  not carry has no detectable header, so such a layout could be mapped by hand and then
  never recognised again, its signature having been computed over an empty list. It now
  falls back to the first row that is not a movement. A profile is keyed on what the
  columns are CALLED, and the file calls them something whether or not we understand it.
- **`ingestFile` never passed the account's currency**, even when the person named the
  account at upload. C3 recorded this as a constraint and left it; it meant a statement
  whose figures name no currency was refused for want of one. The explicit case is now
  wired, and the two that genuinely cannot be answered — an account resolved from the
  statement, and a first import — remain questions.

**Drift** is the case this design exists for. A bank adding a column is the commonest
change there is, and a positional mapping gets it silently wrong: every role shifts one to
the right and the import still looks fine. Matching on labels turns it into a question,
and the wizard now opens pre-filled — the columns it already knew answered, only the new
one blank — with confirmation superseding the old profile rather than leaving two for one
bank, which would make the reading depend on the order the database returned them.

Still unreachable, and honestly so: `recordProfileUse`, `deleteProfile`, `exportProfile`
and `importProfileDocument` want a profile-management screen — listing what is remembered,
forgetting one, and sharing a mapping with someone else. That is a settings surface, not
part of reading a statement.

### Two defects found by asking what was left

Answering "anything outstanding?" against the code rather than the plan turned up two
things the plan did not know about.

**A workbook still imported one account of two.** Three sheets went in — `Account-A`,
`Account-B`, `Notes` — and one statement came out. The cause was a category error rather
than a bug: sheets were being treated as ALTERNATIVE readings, like two encodings of a
CSV or two assemblers on a PDF, where at most one can be right and arbitration picks it.
A sheet per account is not that. They are separate statements, and picking a winner is not
arbitration, it is losing an account — with the content hash recorded, so the corrected
re-upload was then refused as a duplicate.

`readGenerically` now takes what its groups ARE: `choose` for alternative readings,
`collect` for separate parts. The workbook imports both accounts, in their own currencies,
and leaves the sheet that is not a statement alone.

**A currency that was only shaped like one.** The same workbook imported with a currency
of **`SYN`** — the first three characters of the account number `SYN-0001`, which sit
beside a digit exactly as a currency code does. It passed the guard added in C1, because
that guard checked `/^[A-Z]{3}$/` — a shape, not a fact.

Currency codes are now checked against the platform's own locale data, which is where
`minorDigits` already goes to ask how many decimal places a currency has. `SYN` is not a
currency; nor is `PRA`, which an earlier bug read out of "Authorised by the PRA" on a UK
statement. The list is derived rather than written here, so it neither goes stale nor has
to be maintained. The check runs at detection AND at the gate.

Both are pinned as tests.

### Is it universal? — measured, not asserted

The honest test is not the corpus percentage. It is whether the five hand-written adapters
still earn their place: if the engine reads those banks without them, no bank-specific
knowledge is load-bearing.

Measured by bypassing every adapter and standards parser and reading the real corpus with
the generic engine alone:

```
mBank PL csv      5 P4     Fio         2 and 6 P2     Komerční banka     28 P1
mBank PL pdf      5 P4     Revolut         38 P3      mBank CZ           18 P3
UK               10 P4     DE              10 P4      Česká spořitelna  104 P2
CaixaBank       140 P3     Raiffeisenbank ×3  43, 30, 44 P2
```

**Every one of the fourteen importing files reads with no bank knowledge at all, and no
adapter is load-bearing.** The four files still refused are refused the same way with the
adapters in place — a broker report, a holdings snapshot, a statement whose dates are
genuinely undecidable, and one PDF that assembles but cannot prove itself.

Seven defects stood between the prototype assembler and this, every one of them ours:

1. **`NUMBER_SHAPE` and `AMOUNT_LIKE` rejected a leading `+`.** A bank writing credits
   `+249.00` had those rows fail "is this a number", removing them from the table — 100
   498.00 across four movements on one statement.
2. **The shape fallback took the first parseable column and counted coverage on non-empty
   cells.** A column with one populated cell in 104 scored 1 of 1 and claimed `amount`,
   blocking the real one. And `28. 2. 2025` parses as 282202500, so a value-date column
   satisfied the numeric test and, being further left, was reached first.
3. **Header recovery took one line.** Both remaining banks stack their header over three to
   five printed lines, and the nearest is the wrong one — for Raiffeisenbank it labels the
   amount column `Kurz`, the exchange rate.
4. **The fix for (3) then swallowed a balance.** mBank prints `Saldo początkowe: 67,93` as
   ONE cell, so it reads as text and slipped past the "stop at a figure" test.
5. **Region splitting separated one movement from the next.** 58 of 103 adjacent pairs
   shared under half their columns, so a 104-movement table became 23 regions each asked to
   prove itself alone against balances printed once for all of them.
6. **Two columns can both be called "amount", and two can both hold dates.** Coverage
   settles it: the movement's own amount is on every movement, and so is its booking date.
7. **A foreign original beat as a movement.** Raiffeisenbank right-aligns `509 UAH` to the
   same edge as its koruna amounts, so the statement gained a transaction that never
   happened, for 509 CZK. The account's currency separates them — this ledger moves in CZK,
   and a figure labelled UAH is the same money said twice, not more of it. It is now
   recorded as the original of the movement above, which is where `ParsedRow` has always
   said it goes.

**So: yes, on the evidence available.** Every real statement this project holds is read
without knowing which bank wrote it, and every one that is filed proves itself against its
own arithmetic first. The five adapters remain as fast paths and as a second opinion the
arbitration can weigh, but nothing depends on them any more.

Two honest qualifications, because "universal" should not be heard as "reads anything":

- **A format that carries no balances cannot be verified, and is refused.** QIF states a
  date, an amount and a payee, and nothing that could check them. That is a property of the
  format, and taking it on trust is the one thing this reader does not do.
- **Twenty of 294 synthetic files are still refused**, each named and understood. The
  largest group is that QIF; the rest are dormant statements with nothing to read, two
  undecidable decimal separators, and a spreadsheet format not yet implemented.

### Where the numbers landed

Real corpus — unchanged at 11 importing files and identical row counts, now with
currency asserted:

```
mBank PL csv   5 PLN     ČS      104 CZK     RB  43/30/44 CZK
mBank PL pdf   5 PLN     Revolut  38 CZK     UK   10 GBP     <- pdf is new
Fio 2 + 6 CZK            DE       10 EUR     CaixaBank 140 EUR
```

Twelve importing files, up from eleven. The mBank PL **PDF** now reads identically to the
CSV its hand-written adapter reads — same five amounts, same balances, same P4 — with no
bank-specific code at all. Its fingerprints still differ from the CSV's, because those
include descriptive text the two formats word differently, so the plan's cross-format
fingerprint equality remains a target rather than a result.

Synthetic corpus — now committed to `tests/fixtures/synthetic/` (1.5 MB, 354 files) and
enforced by `tests/acceptance/synthetic-corpus.test.ts`, scored on exact amounts AND
currency:

```
FORMAT      files   correct   gaps        was (before Phase C)
csv          120       117       3        3
tsv           60        57       3        0
pdf-text      57        53       4        0
camt053       19        19       0        19
mt940         15        15       0        15
abo            3         3       0        3
ofx           10        10       0        not implemented
qif            8         0       8        refused by design — carries no balances
xlsx/ods       2         0       2        one workbook drops a sheet; ODS unread
```

**Fully correct: 37 → 274 files.** Of the 57 remaining gaps, 30 are one malformed CSV
shape (comma as both delimiter and decimal, unquoted), 19 are formats not implemented,
and 8 are individually named and understood.

Every file is in one list or the other, and **both directions fail**: a gap that starts
working breaks the suite, because the reason recorded against it has become false.

---

## The governing rule

> **No preset may ever be the reason a statement is accepted. A preset can only make
> acceptance _fast_. Acceptance itself is always arithmetic.**

**Status: delivered.** Every route into the ledger meets the same arithmetic — the five
adapters, the four standards parsers and the generic readers alike — and a P0 statement is
refused whatever vouches for its columns. The measurement that matters is below under
_Is it universal?_: no bank-specific knowledge is load-bearing any more.

What follows records how it was not delivered, because the shape of that failure is the
reason the rule exists.

`proveStatement` has exactly **one** call site in the whole codebase — `detect.ts:343`,
inside `readGenerically`. Everything else reaches the ledger without arithmetic:

| Route                                                    | Files                           | Gated? |
| -------------------------------------------------------- | ------------------------------- | ------ |
| Generic tabular / PDF reader                             | `detect.ts:343`                 | yes    |
| Five hand-written adapters (fio, revolut, mbank, rb, cs) | `detect.ts:163-173`, `:235-242` | **no** |
| Three standards parsers (camt053, mt940, abo)            | `detect.ts:126-141`             | **no** |

That is not theoretical. Tampering with real samples — deleting one movement from a
statement that prints its own closing balance — is accepted on every ungated route:

```
ACCEPTED  fio, one row deleted       rows=5   endpointGap=2 976 000  autoImport=false
ACCEPTED  mbank, one row deleted     rows=4   endpointGap=30 000     autoImport=false
ACCEPTED  revolut, one row deleted   rows=37  endpointGap=null       autoImport=TRUE
ACCEPTED  mt940, one movement short           endpointGap=-541 590
ACCEPTED  camt.053, 100.00 against a 4000.00 balance change  endpointGap=390 000
```

`autoImport=false` is not a safety net here — nothing asks the user, because the
question flow lives on the generic route too. The only arithmetic check these routes
meet is `assertRowsExplainTheBalance` (`detect.ts:37-46`), which fires only when a
statement has **zero** rows _and_ differing endpoints. One row short of a hundred
sails through.

And the rule is contradicted by the plan's own Phase 4 policy table. `proof.ts:355-360`
returns `autoImport: true` for a verified profile **before** any proof class is
consulted:

```
REFUSED  no profile:                    ... nothing in the statement could be checked
REFUSED  unverified saved profile:      ... nothing in the statement could be checked
ACCEPTED verified saved profile:  rows=3 proof=P0 opening=undefined closing=undefined
```

A P0 statement — no opening balance, no closing balance, no totals, no running balance —
imports because a preset said so. That is the rule's exact counterexample, written into
the policy table at line "P0 → ask, or use a verified saved profile". One of the two has
to go. **The rule stays; the policy line goes.**

(This path is unreachable in production today only because nothing ever writes a
profile — see Phase 5.)

### The two honest qualifications, revisited

- **For tabular formats (CSV/XLSX) generic discovery is strong.** Confirmed and
  unchanged. A reader with no bank knowledge finds mBank's and Revolut's amount and
  balance columns purely by arithmetic, including Revolut's `Amount − Fee` composite.
- **For PDF layouts it was called materially weaker. That claim is now disproven.** The
  limitation written into `frompdf.ts:17-20` and `corpus.test.ts:162-176` — that
  multi-line transaction records are a structural boundary — is **false**, and must be
  deleted rather than softened. A second assembler reads all four "impossible" files,
  each confirmed against its own printed totals. See _Two readers, one arbiter_.

---

This document is the release narrative — how each part came to be, and what was
wrong before it. **How the reader works today, and what it still cannot read, is
in [`statement-import.md`](./statement-import.md)**, which is the one to reach for
when picking this up later.

The interface these phases imply is specified separately in
[`v0.3.8-ui-spec.md`](./v0.3.8-ui-spec.md); house conventions are in
[`ui-guidelines.md`](./ui-guidelines.md).

---

## Where this stands

Nothing is committed. The working tree holds roughly 3 800 new lines and 530 changed
ones. `npm test` (1213 passing), `npm run check`, `npm run lint` and `tsc --noEmit` are
all green — the failures below are things the suite does not look at, not a broken build.

| Phase                          | Status                                                                                                  |
| ------------------------------ | ------------------------------------------------------------------------------------------------------- |
| 0 — immediate wins             | **done**                                                                                                |
| 1 — format-first router        | **done**; safety boundary is **defeatable**, multi-statement works for CAMT and **fails for workbooks** |
| 2 — standards parsers          | **done** for CAMT.053 / MT940 / ABO; **OFX not implemented**                                            |
| 3 — lossless tabular engine    | **mostly done**; merged cells not implemented, candidate generation narrower than described             |
| 4 — proof engine               | **built, not wired**; reachable from one of eight routes                                                |
| 5 — profiles, wizard, drift    | **server primitives only, zero callers**; no user can create a profile                                  |
| 6 — PDF and OCR arbitration    | PDF **done**; OCR **built, unreachable, and has never read a statement**                                |
| 7 — async pipeline, provenance | not started                                                                                             |
| 8 — GUI-blocking audit         | not started                                                                                             |

### What genuinely works

Measured, not asserted. Eleven of nineteen corpus samples import, 432 rows in total, and
every one of them agrees with its own arithmetic:

- nine samples close exactly on `opening + Σ(amount − fee) = closing`
- Revolut (38 rows) and CaixaBank (140 rows) print no endpoints and instead carry a
  complete running chain
- zero samples import without some arithmetic agreement

Three PDFs — UK, German, CaixaBank — are read with **no bank-specific code at all**,
by glyph geometry and arithmetic alone. CaixaBank is 140 rows joined across eight pages.
That is the part of the thesis that is proven.

Also genuinely delivered:

- Raiffeisenbank's January template, which read **zero rows** before this branch, now
  reads 43 and reconciles. Regression fixture committed.
- pdf.js detaches the buffer it is handed. `pdftext.ts:10` now passes a copy — before
  this branch **every stored original PDF was written to disk as 0 bytes.** A
  pre-existing production bug, fixed here, worth crediting on its own.
- The five adapters produce byte-identical `ParsedRow[]` to their pre-branch versions on
  every real sample, verified against `4626934`. The only difference is the intended RB
  January fix.
- Nothing calls home. Verified by sealing `fetch`, `http/https.request`, `net.connect`
  and `net.Socket.prototype.connect` and running the full OCR path: **0 outbound
  attempts.** `langPath` is absolute, so tesseract.js takes its local-file branch and
  never reaches the jsdelivr default.
- Macros and formulas are never executed (`bookVBA: false`, `raw: false`).
- A per-file corpus suite exists, naming what each sample must do and why — written
  because four changes in a row each fixed one statement and broke another while the
  aggregate pass count stayed flat.

### What is broken, in severity order

**1. A wrong import happens today.** `KU_3_month_bank_statment.pdf` — CaixaBank, 140
transactions, denominated in euro — imports as **CZK**, at P3, auto-imported, and the
corpus suite is green because it never asserts currency.

```
CaixaBank imports as currency: CZK   rows: 140
first row currency: CZK
€ glyphs in the file: 66
```

The cause is `statement.ts:186-192`. The symbol test matches symbol-_then_-digit, which
is the US and UK order and almost nowhere else in Europe:

```
"600,41 €"      -> NOT DETECTED       "€ 600,41"           -> €
"1 234,56 Kč"   -> NOT DETECTED       "Balance 600.41 EUR" -> EUR
"500 zł"        -> NOT DETECTED
"12 500 Ft"     -> NOT DETECTED
```

Detection fails, and `statement.ts:433` finishes the job:

```ts
const currency = options.currency ?? evidence.currency ?? 'CZK';
```

A default currency is a fabricated fact about someone's money. It must not exist:
undetected currency is a **question**, never a guess.

The root cause is deeper than the regex, and the repair is in _Currency and bank come
from the account_: the importing account already states its currency, so the document
should never have been the source. The regex fix stays worth making — for the
cross-check and for a first import with no account yet — but it stops being what stands
between the user and a wrong number.

**2. Multi-currency rows are stored 100× wrong.** `statement.ts:434` derives minor-unit
digits from the _statement's_ currency while `:477` labels each row with its _own_
currency. A HUF row inside a CZK statement:

```
file says   3 000,00 HUF   (= 3000 minor, HUF is zero-decimal)
stored as  -300000 HUF
proof P3, autoImport=true
```

Deriving digits per row is the wrong repair. An account has **one** ledger currency —
`account.currency` is a single column and `resolveAccount` refuses a mismatch — so a HUF
line inside a CZK statement is a CZK movement with a foreign original, not a HUF
movement. Fixed properly by the same change: _Currency and bank come from the account_.

**3. A transaction whose description contains a summary word is silently deleted.**
`regions.ts:169-176` filters rows by `looksLikeSummary` _before_ `readTabular` counts
them, so the drop is invisible to the question logic. Reproduced: 3 rows in, 2 rows
stored, 0 questions asked, P3, `autoImport=true`.

There is a nuance neither review caught. In production that fixture is currently
_refused_ — but for the wrong reason. `detect.ts:285` gathers evidence across **all**
candidate readings (`candidates.flatMap((grid) => detectRegions(grid))`), so one
reading's balances are used to judge another's rows, manufacturing a spurious
contradiction. **One bug is masking the other.** Fixing the evidence pollution without
fixing the row filter turns a refusal into a wrong import.

**4. P3 with no printed opening balance proves nothing about the head of the file.**
`testChain` (`proof.ts:105-108`) anchors to an opening balance only when one exists.
Revolut and CaixaBank print none, so deleting their _first_ row leaves the chain closing
perfectly. That is 178 of the corpus's 432 imported rows, auto-imported on an unanchored
chain.

**5. The decompression-bomb gate reads attacker-controlled numbers.**
`assertSafeToParse` (`safety.ts:100-149`) trusts the zip **central directory**:

```
honest 240 MB expansion, ratio 1029       -> REFUSED (correctly)
directory declares 0 bytes uncompressed   -> ACCEPTED
directory declares 12 KB per entry        -> ACCEPTED
```

And SheetJS inflates it regardless: a **299 KB upload moved RSS by 812 MB in 586 ms**.
The declared sizes are a courtesy, not a bound. Also missing entirely: sheet, row,
column and cell caps, a processing-time ceiling and a memory ceiling — all promised in
Phase 1, none implemented. Zip inspection covers only `xlsx`/`ods`; a PDF gets a 32 MB
length check and nothing else.

**6. A workbook with one sheet per account loses accounts silently.**
`readGenerically` (`detect.ts:294-299`) sorts candidate readings and returns
`readings[0].statements` — one winner, and for a workbook that is one candidate _per
sheet_:

```
sheets in the workbook: Cuenta 1111, Cuenta 2222
statements returned: 1   (rows=3)
```

The second account's movements vanish, `totalRows > 0` so ingest proceeds, and the
content hash is recorded — so the corrected re-import is later refused as a duplicate.
That is precisely the failure mode this release exists to eliminate, reintroduced in a
new place. Arbitrating candidate readings by `max(rowCount)` is wrong in principle:
**disagreement between readings is a question, not a contest.**

**7. OCR has never read a statement.** 15 attempts across every ground-truth render and
all five refused PDFs: **15 refusals, 0 statements.** The German page that reaches P4
from its text layer reaches **P0** from pixels. Nothing sets `options.ocr: true`
anywhere in `src/`, so the path is unreachable in production regardless, and no test in
`tests/` exercises it. The image formats advertised in the upload dialog
(`.png,.jpg,.jpeg,.heic`) therefore always fail. The safe half holds — it degrades to a
refusal, never to a wrong import — but "arbitration" is unearned.

**8. Phase 5 is unreachable code.** `saveProfile`, `profileFromReading`,
`saveDriftedProfile`, `exportProfile`, `importProfileDocument`, `deleteProfile` and
`recordProfileUse` have **zero callers** in `src/`. Only `loadProfiles` is called.
`BUILTIN_PROFILES` is `[]`. There is no wizard and no drift dialog — `src/routes/(app)/import/`
holds a `+page.server.ts` and a `+page.svelte` and nothing else. `import_profile` is
always empty in production, which is the only reason defect (1)'s verified-profile
bypass has not fired.

**9. Two geometric bugs with named line numbers.** `frompdf.ts:235` compares against
`previous.y`, which is never advanced when lines are absorbed (`:238-243` pushes cells,
xs and xEnds but not y), so the absorption window drifts. `regions.ts:96-102` splits
regions on exact populated width, which is fatal for ragged geometry-derived grids.

---

## Corrections to the record

Claims in the previous draft that the code contradicts. Listed so they are not
re-asserted from memory later.

| Claimed                                                                               | Actual                                                                                                                                                                                                                                           |
| ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| OFX/QFX support                                                                       | Sniffed only (`format.ts:111`); `detect.ts:108` says "being built". No `standards/ofx.ts`.                                                                                                                                                       |
| Five adapters migrated onto the engine                                                | Not done. They remain the primary route and bypass the gate.                                                                                                                                                                                     |
| Merged-cell handling                                                                  | `worksheet['!merges']` is never read.                                                                                                                                                                                                            |
| Filename-pattern profile matching                                                     | `schema.ts:240` defines the column; `matchProfile` keys on header signature only. Dead column.                                                                                                                                                   |
| `FINGERPRINT_VERSION` → 4                                                             | Still 3 — **correctly.** No row's fields moved, so a bump would be gratuitous. The plan was wrong, not the code.                                                                                                                                 |
| Candidate generation across encoding × delimiter × quote × header row × footer cutoff | Only encoding × delimiter are enumerated (`grid.ts:116-126`). Quote is fixed at `"`. Header row and footer cutoff are _derived_ by `regions.ts`, not enumerated.                                                                                 |
| Multilingual header dictionary as a data file                                         | It is `export const HEADER_TERMS` in `tabular/vocabulary.ts`. Defensible — but say so.                                                                                                                                                           |
| Built-in profiles as JSON files                                                       | `BUILTIN_PROFILES = []`. `profiles.ts:22-30` argues well for why; promote that reasoning here.                                                                                                                                                   |
| Holdings snapshots routed to investments                                              | `detect.ts:178` _throws_ an explanatory message. Nothing routes. The check also runs only inside the PDF branch, so an XLSX broker report never reaches it.                                                                                      |
| Every bank prints opening and closing balances                                        | Revolut prints neither.                                                                                                                                                                                                                          |
| Schema: a `statement` table with proof and evidence columns                           | No such table — it is `import_file`, and none of those columns exist. `0043_import_profiles.sql` is the only new migration. **No proof class or provenance is persisted anywhere**, so Phase 7's provenance filter has no data to stand on.      |
| `BankId` and `BANK_LABEL` replaced by a record with country                           | Half done: `BankId` is `string` (`types.ts:9`), `BANK_LABEL` (`ingest.ts:80`) is still a flat `Record<string, string>`.                                                                                                                          |
| Golden fixtures — identical `ParsedRow` sets                                          | `corpus.test.ts` pins row _counts_, proof class, endpoint reconciliation and fingerprint _uniqueness_. Only `rb-lines-jan2025.json` is a stored fixture.                                                                                         |
| Cross-format fingerprint equality for the mBank CSV and PDF                           | Impossible as written — the PDF is a documented gap and is refused. Restate as a target.                                                                                                                                                         |
| OCR: rendered page reconciles to P4                                                   | Every ground-truth render reaches P0 and is refused.                                                                                                                                                                                             |
| `package.json` untouched until approved                                               | Stale. `mupdf` and `tesseract.js` are real dependencies; `fetch:tessdata` is a script. Approved since.                                                                                                                                           |
| Phase 1's `+page.svelte:43` lie fixed                                                 | Partly. `.ofx` is gone; `accept` still advertises `.xls` (rejected), `.xml` (non-CAMT rejected) and four image types that always fail.                                                                                                           |
| Phase 0's zero-row hard error                                                         | Delivered but narrower: `assertRowsExplainTheBalance` needs opening **and** closing **and** a difference. The general case is caught by `ingest.ts:515-527` without recording the hash — that is the part that actually closes the original bug. |

---

## Phase C — Consolidation (next, and before any new parsing)

The branch has more reading ability than it has safety. Every item here closes a hole
that is open right now; none of it adds a bank.

1. **One gate, no exceptions.** Route the adapters and the standards parsers through
   `proveStatement`/`decideImport`. A trusted mapping may skip _discovery_; it may not
   skip _arithmetic_. Where a parser produces a statement whose own printed balances
   disagree with its rows, that is a question, not an import.
2. **Move the `verifiedProfile` branch below the proof-class check** (`proof.ts:355`)
   and delete the "or use a verified saved profile" line from the P0 policy.
3. **The account is the authority for currency and bank — not the document.** See
   _Currency and bank come from the account_ below. Delete the `'CZK'` default
   (`statement.ts:433`) outright; where no account is known yet, currency is a
   **question**, never a guess.
4. **The ledger amount is in the account's currency, full stop.** A row's own currency
   column describes an _original_ foreign amount, which is a separate fact, not the
   movement's denomination.
5. **Count summary-filtered rows and surface them.** A dropped row is either explained
   or asked about; it is never silent.
6. **Scope `readEvidence` to the reading it proves** (`detect.ts:285`) — fix this
   _together with_ item 5, since it is currently masking it.
7. **Anchor P3.** A running chain with no printed opening balance may not auto-import;
   it can prove internal consistency and nothing about its head.
8. **Replace `max(rowCount)` arbitration with disagreement-is-a-question**, and return
   every sheet's statement from a workbook rather than one winner.
9. **Bound inflated bytes during reading, not declared ones**, and add the sheet, row,
   column, cell, time and memory caps Phase 1 promised.
10. **Assert field values in the corpus suite** — currency at minimum, and reconciliation
    for all eleven importing files rather than nine.
11. **Land or delete the unreachable code.** OCR and the profile store are either wired
    to a consumer or removed; unreachable code that appears finished is worse than
    absent code, because it reads as a delivered promise.
12. **Fix `frompdf.ts:235` and `regions.ts:96`** — required regardless of which
    assembler ships.
13. **Add a range check to `isDateLike`** (`determinacy.ts:26`) so money is not read as
    dates, and stop treating an unseparated run of digits as money. Both are small,
    both have a measured effect, and neither depends on anything else here.
14. **Then, and only then, add the rhythm assembler as a second candidate reading**
    (`scratch-workspace/probe/rhythm.ts`). Strictly after items 1–8: a second reader in
    front of a gate that is not yet universal is a second way to be confidently wrong.
    Delete the false limitation in `frompdf.ts:17-20` and the four GAP entries in
    `corpus.test.ts:162-201` as part of this, replacing them with real expectations.

---

## Phases

Each phase ends green: `npm run check && npm run lint && npm test`.

### Phase 0 — Bank the immediate wins — **done**

- `rb.ts:4` and the period matcher at `rb.ts:51` accept both `3.1.2025` and `1. 3. 2025`.
- `KS_Bank_01:2025.pdf` is a regression fixture asserting 43 rows and exact
  reconciliation.
- A zero-row parse from a statement printing balances is a hard error — see the caveat
  in _Corrections_.
- `ParsedStatement` gained `statedCreditTotalMinor`, `statedDebitTotalMinor`,
  `statedRowCount`, populated in the five adapters. They are **never read**, because no
  proof runs on that route — Phase C item 1 is what gives them a purpose.

### Phase 1 — Format-first router and the safety boundary — **done**

- `detect.ts` routes on format sniffed from bytes, then bank. Done.
- Safety boundary: done. The declared sizes are read first because they give the clearest
  message when honest, and then every entry is actually inflated under a shared byte
  budget, so a lying central directory no longer passes. Sheet and cell caps added; the
  walk is time-bounded.
- `ingestFile` returns many statements per file: **true for CAMT**, still false for
  workbooks — the one part of this phase that remains open.

### Phase 2 — Standards parsers — **done**

| Format   | Status                                                     |
| -------- | ---------------------------------------------------------- |
| CAMT.053 | done, ISO 20022, Czech Banking Association dialect handled |
| MT940    | done                                                       |
| ABO/GPC  | done                                                       |
| OFX/QFX  | **not implemented — moved out of v0.3.8**                  |

ABO's three traps, all confirmed from ČS document 3-2267 and all handled: amounts are
already in haléře; the sign is the posting code (`1` debit, `2` credit, `3`/`4`
_reversals_); account numbers may be permuted into vnitřní formát with the
counter-account's bank code hidden in the K-symbol field at positions 5–8 from the right.

### Phase 3 — Lossless tabular engine — **done**

Delivered: extraction as strings with `displayText` preserved; region detection before
table parsing (mBank's most common row width is 2 — its metadata pairs — while its
transactions are width 8 two-thirds down the file); regions classified as metadata /
summary / transactions / footer with summary kept as evidence; a semantic exclusion rule
for rows like `;;;;;;#Saldo końcowe;106,59 PLN`.

Since delivered: the semantic exclusion rule no longer deletes real transactions silently
(C5), CSV is split into records rather than lines so a wrapped description no longer
fragments a table, and over-split rows are rejoined as a candidate reading the arithmetic
rules on.

**Merged-cell expansion: done.** A spreadsheet writes one heading across a group of
columns — `Amount` over a `Debit`/`Credit` pair — and stores it in the top-left cell only,
so the row arrived as a label followed by blanks and every column but the first lost its
name. Horizontal merges are now spread across the columns they span.

Only horizontal, and that restriction is the whole safety of it: a merge across columns is
a label written wide, while a merge DOWN rows is one value applying to a group, and
copying it into each of them would manufacture data — a date repeated onto movements that
never carried one, which the reader would then file without hesitation. Pinned in
`tests/unit/merged-cells.test.ts`, including that direction.

Narrower than claimed: candidate generation enumerates encoding × delimiter, plus the
rejoin repair. The quote character is fixed at `"`, and the header row and footer cutoff
are derived by `regions.ts` rather than enumerated.

### Phase 4 — The proof engine — **done**

The engine was always sound; its problem was reach, and that is fixed. Every route into
the ledger now meets it — the five adapters, the three standards parsers and the generic
readers alike — and a P0 statement is refused even when a confirmed profile vouches for
its columns.

**Ledger models are enumerated hypotheses, never assumptions:** signed amount column;
`credit − debit` **and** `debit − credit` pairs; amount net of a separate fee
(`Amount − Fee`, Revolut); ascending **and** descending row order; balance-after /
balance-before / available / ledger.

**Proof classes, ranked lexicographically** — proof class first, heuristic score only
ever breaks ties:

|        | Evidence                                                                                       |
| ------ | ---------------------------------------------------------------------------------------------- |
| **P4** | running chain + opening + closing + stated totals + row count                                  |
| **P3** | complete running chain only — **must be anchored to a printed opening balance to auto-import** |
| **P2** | aggregate closure; stronger when independent debit **and** credit totals agree                 |
| **P1** | endpoints only                                                                                 |
| **P0** | no arithmetic evidence                                                                         |

**Lexical constraints gate every class.** Arithmetic closure alone is not proof: a
uniform 1000× misreading of amounts _and_ balances closes the chain perfectly. Require
consistent decimal and grouping, monetary precision compatible with the currency,
displayed text agreeing with the inferred scale, identifier columns never reinterpreted
as money, and consistent negative markers.

**Sign is derived from the balance movement wherever a balance column exists**; a
printed `S`/`H`/`−`/`( )` is corroboration only. Evidence: OCR read `87,45 S` as
`87,45 Ss` and a strict match dropped the whole row while the amount itself was perfect.
One glyph must never decide a transaction's direction.

**Auto-import policy** (revised — the P0 escape hatch is gone):

```
P4 / P3-anchored + lexical checks pass   -> import silently
P2 / P1 + every dimension determined     -> import silently
P2 / P1 + one dimension undetermined     -> ask about THAT DIMENSION ONLY
P3-unanchored                            -> ask
P0                                       -> ask
```

Determinacy tests, all proven against real files: **date order** — any component > 12
settles it, else the stated period settles it (the US fixture has 14 dates all ≤ 12 and
is resolved only by "March 1, 2025 through March 31, 2025"), else ask; monotonicity is
not evidence, because banks post in operational order. **Decimal separator** — a value
carrying both settles it; a 3-digit group beside 2-digit groups settles it. **Amount vs
balance** — if one column's consecutive differences equal another column's values, both
are identified at once and the file promotes to a chain proof.

### Phase 5 — Profiles, wizard, drift — **done**

Reachable at last. `previewLayout` returns what the reader saw in a file it could not
file, the wizard asks one question of it, and `confirmMapping` saves the answer and reads
the file back through the same arithmetic as any other statement.

- Built-in profiles: **none, deliberately** — `profiles.ts:22-30` argues that a shipped
  profile is a preset by another name, which the governing rule forbids as a basis for
  acceptance. Keep the argument, drop the "JSON data files in the repo" plan.
- Matched by **column signature — never by position.** Firefly III's positional `roles`
  array breaks the moment a bank adds a column. Filename-pattern matching is specified
  but unimplemented; implement it or drop the column.
- Profile format is a strict superset of the prior art: encoding, header-name mapping,
  balance role, debit/credit pair, sign convention, decimal and grouping style, currency
  column, period source.
- **Drift re-opens the wizard pre-filled with the previous mapping** — "this export
  changed since last time" — never a bare error. Done: a layout that nearly matches
  arrives with the columns it already knew answered, only the new ones blank, and
  confirming supersedes the old profile rather than leaving two for one bank.
- Export/import a profile as JSON. Implemented, uncalled.
- Multilingual header dictionary (`Datum` / `Fecha` / `Data` / `Dátum`, `Betrag` /
  `Importe` / `Kwota` / `Částka`, `Verwendungszweck` / `Concepto` / `Tytuł` / `Zpráva
pro příjemce`). Column role comes from this dictionary **first**, value shape second —
  Fio's `Protiúčet` (`2600247495`) classifies as numeric and must never be offered to
  the arithmetic as an amount. Delivered as `HEADER_TERMS` in `tabular/vocabulary.ts`.

### Phase 6 — PDF and OCR arbitration — **done**

Delivered and working: amount assembly **spans cells** (CaixaBank's `-1 000,00 €`
arrives as `-1 § 000,00 § €`), avoiding the inverse trap where "space then three digits"
also describes the gap between two columns (`300,00 377,93`) — only join when the run
before the space does not already carry a decimal. Right-edge anchoring for money
columns. Three PDFs read with no bank-specific code.

Not delivered: adapter migration (Phase C item 1); holdings-snapshot **routing** (it
throws instead, and only on the PDF branch).

OCR: it reads statements. Recognition was never the problem — two defects in this
repository's own handling of the result were, and both are fixed (see _OCR — it works_
above). 8 of 20 synthetic statements read exactly, everything filed is exact, and it is
reachable through the queue, which is where seconds-per-page work belongs. Statement-level arbitration with **no
cell-level splicing** — a spliced row exists in neither source read — is implemented as
designed. `mupdf` rasterises (WASM, no native addons, verified on this Alpine base);
`tesseract.js` reads `eng ces pol deu spa` from bundled data. Row extraction uses word
bounding boxes and column geometry, not text-line splitting, and words are joined into
cells before any of it — a phrase is one cell, as it is in a text layer. One latent hole: `ocrAvailable()` (`ocr.ts:37`)
checks only that the `tessdata` directory exists, not that the five files are in it, so
a partial fetch throws a confusing ENOENT.

### Phase 7 — Async pipeline and provenance — **done**

- Deterministic parsing stays inline and instant. Anything requiring rasterisation is
  **queued**; the upload returns immediately. Nothing blocks the GUI, ever.
- Queue shows depth and per-file progress; multiple files queue visibly.
- Single lease, one file at a time — tesseract is CPU-bound and parallel workers would
  starve the web server on a NAS. Reuse the lease-locking pattern in
  `calendar/sync/engine.ts`.
- **Provenance per row**: how it was read, which proof passed, source file, and
  byte-line or sheet-range. Surfaced on the transaction and as a filter. **Nothing is
  persisted today** — this phase must add the columns before the filter can exist.

Phase 6's OCR could not be marked done independently of this phase, and that dependency is
now discharged: the queue exists, `ingestFile` takes an `ocr` option, and only the queue
passes it.

### Phase 8 — GUI-blocking audit — **done**

Sweep every server operation for work that blocks the interface — calendar sync, backups,
FX refresh, net-worth recomputation — and hold each to the same never-block rule. Report
findings even where no fix is needed.

---

## Schema

Migrations continue from `0043_import_profiles.sql`, which is so far the only one and
adds `import_profile` alone. Everything below is **pending**, not done.

- `import_file` (there is no `statement` table) — `stated_credit_total_minor`,
  `stated_debit_total_minor`, `stated_row_count`, `proof_class`, `ledger_model`,
  `reconciliation_evidence` (jsonb).
- `import_profile` — exists: id, name, bank, country, format, column signature, filename
  pattern, mapping (jsonb), version, verified flag, origin.
- `transaction` — `source_method`, `proof_class`, `source_ref` (file, sheet/page, row).
- `BANK_LABEL` (`ingest.ts:80`) becomes a record with id, display name and country.
  `BankId` is already `string`.
- `FINGERPRINT_VERSION` stays at **3**. No fingerprint-affecting change occurred.

---

## Tests

**Unit tables — cheaper and more targeted than fixtures.**

| Convention             | Case                     | Status                                                                                     |
| ---------------------- | ------------------------ | ------------------------------------------------------------------------------------------ |
| Zero-decimal currency  | HUF `12 500 Ft`          | works via `amountFromCell`; `parseAmount` returns null — write the test against the former |
| Apostrophe grouping    | CHF `1'234.55`           | covered                                                                                    |
| Lakh grouping          | INR `12,34,567.89`       | works, **no named test**                                                                   |
| Space grouping         | `54 650.00`, `-1 000,00` | covered                                                                                    |
| Parenthesised negative | `(48.71)`                | covered                                                                                    |
| Trailing minus         | `1 234,56-`              | covered                                                                                    |
| Trailing-dot date      | `2025.03.14.`            | works, **no named test**                                                                   |
| Timestamp date         | `2026-06-30 09:32:29`    | works, **no named test**                                                                   |

**The corpus suite** (`tests/acceptance/corpus.test.ts`) names every sample and what it
must do, and fails with both names on screen when a change trades one statement for
another. It must additionally assert **currency** and **field values** — its silence on
currency is why a 140-row euro statement imports as CZK with the suite green.

**Regressions with a named cause:**

- `KS_Bank_01:2025.pdf` — RB January template, non-zero rows and exact reconciliation. ✅
- mBank footer row must not import as a phantom transaction. ✅
- Fio `Protiúčet` must not be classified as an amount. ✅
- Revolut `Amount − Fee` composite must be discovered. ✅
- Robinhood holdings snapshot must route away, not fail to reconcile. Refused with an
  explanatory message; routing is pending.
- **Deleting one movement from any statement that prints its own balances must be
  refused, on every route.** Currently accepted on eight of them.
- **A transaction whose description contains a summary term must not be deleted.**
- **A statement in a currency the reader cannot determine must ask, not assume.**
- **A workbook with two account sheets must yield two statements.**
- Cross-format fingerprint equality for `72970193_260701_260731.csv` and `.pdf` — a
  target, not a current test; the PDF is refused.
- OCR: a rendered page must reconcile. Currently 15 attempts, 15 refusals — this test
  would fail today and should be written anyway, marked as the known gap.

---

## Dependencies

`mupdf` and `tesseract.js`, both real dependencies now, both approved. Language data
fetched at build time by `npm run fetch:tessdata` into a gitignored `tessdata/`; the
Dockerfile runs it in the build stage and copies the directory into the runtime image.
The `_fast` models are used deliberately: measured against the full ones they matched
exactly on German (50/50 amounts) and Spanish (41/41), did slightly better on Czech, ran
about 40% quicker, and are 7.4 MB against 40 MB.

---

## Currency and bank come from the account

A statement is never read in a vacuum. It is imported **into an account**, and that
account was created by the user, who stated its currency and its bank. That metadata is
the authority. The document is corroboration — exactly as a printed `S`/`H` marker
corroborates a sign that the balance movement decides.

This inverts the current flow, which guesses first and validates second:

```
today:     parse -> guess currency -> resolve account -> compare -> blame the user
corrected: account -> currency -> parse -> compare with the document -> ask on conflict
```

Most of this already exists and is simply not connected:

- `statement.ts:433` reads `options.currency ?? evidence.currency ?? 'CZK'`. The account's
  currency belongs in that first slot. Today only a saved profile ever fills it
  (`detect.ts:328`).
- `resolveAccount` (`ingest.ts:139-149`) already refuses when `chosen.currency` or
  `chosen.bank` disagrees with the statement. It is a good check pointed the wrong way:
  it currently adjudicates our own fabricated guess.
- `+page.svelte:27` already sends `accountId`.

What this fixes at the root, rather than papering over:

- **The live wrong import.** CaixaBank into a euro account becomes EUR because the
  account says EUR. No symbol regex is on the critical path. (The bidirectional symbol
  fix is still worth making — for the cross-check, and for a first import that has no
  account yet — but it stops being load-bearing.)
- **The 100× multi-currency bug.** `ParsedRow` already encodes the correct model
  (`types.ts:24-26`):

  ```ts
  /** Original amount for FX card payments billed in the account currency. */
  originalAmountMinor?: bigint;
  originalCurrency?: string;
  ```

  The ledger amount is in the account's currency and the foreign original is a separate
  pair of fields. `statement.ts:477` breaks that contract by writing a row's foreign
  currency into `currency`. The defect is field confusion, not a missing per-row digit
  rule — the generic reader must populate `originalCurrency`/`originalAmountMinor` and
  leave `currency` as the account's. Multi-currency Revolut pockets are separate
  accounts, which is already how the system models them.

- **Bank routing.** A known account names its bank, so the adapter is selected rather
  than sniffed. This does not violate the governing rule: choosing a reader is
  _discovery_, and discovery may be fast. Acceptance stays arithmetic.

Two constraints this introduces, both real:

1. **Order of operations.** Currency determines minor-unit digits, which determine the
   parsed integers. When the user picks the account at upload, currency is known before
   parsing and everything is simple. When the account is auto-resolved by account number
   — which happens _after_ parsing — amounts must remain lossless until the account is
   known, and be scaled once. This is the plan's own "extract as strings; never coerce
   during extraction" rule, not yet carried through to the amount scale.
2. **The first import for a new account.** There is no account metadata yet, so the
   document is the only source. This is precisely where a **question** belongs — the
   wizard confirms currency and bank while creating the account — and precisely where a
   guess is least acceptable, because the guess becomes permanent metadata that every
   later import is validated against.

Conflict handling: when the account says EUR and the document plainly says CZK, that is
not a parse failure and not a silent override. It is a genuine disagreement about which
account this statement belongs to, and it is a question — which `resolveAccount` already
words well, once it is comparing a _read_ currency instead of a _manufactured_ one.

---

## Two readers, one arbiter

The multi-line question is settled: **it was never a structural limit.** A second
assembler — prototype in `scratch-workspace/probe/rhythm.ts` — reads every file the
current reader refuses.

It works by never classifying a line. Instead it asks what the page's record _beat_ is:
cluster cells into columns (left edge for text, right edge for figures), find
type-pure regularly spaced recurrences inside one column, treat each such run as a
hypothesis about the beat, and let content **hang below** its anchor — band _i_ is
(anchor _i+1_, anchor _i_]. A hypothesis is valid when the table it implies has one
figure per record in a money column and at least one date per record; among valid
hypotheses, the one accounting for the most cells wins. Nickel is the case that proves
the design: its row-number column implies an invalid table, its amount column implies a
valid one, and no per-layout switch is needed — the wrong anchor simply fails
validation. Results are stable across column tolerance 3–12 pt.

Two other approaches were measured and rejected on the real data, which is worth
recording so they are not retried: whitespace-band segmentation (no threshold exists —
Sparkasse separates records by 14 pt and their own lines by 12 pt, and Nickel vertically
_centres_ rows so intra-record gaps run smaller than inter-record ones), and template
induction with sequence labelling (breaks on Nickel, where the line carrying the date is
not the line carrying the amount).

### Neither reader dominates

| File                          | Ships today                  | Rhythm assembler |
| ----------------------------- | ---------------------------- | ---------------- |
| `RK_Bank_01` — Komerční banka | **refused**                  | 28 ✓             |
| `RK_mbank_08_24` — mBank CZ   | **refused**                  | 18 ✓             |
| `72970193` — mBank PL         | **refused**                  | 5 ✓              |
| `RK_3_month` — Nickel ES      | **refused**                  | 11 ✓             |
| `UK_Statement_04`             | 10 ✓                         | 10 ✓             |
| `DE_Kontoauszug_03`           | 10 ✓                         | 10 ✓             |
| `Account_statement` — ČS      | 104 (adapter)                | 104 ✓            |
| `KS_Bank_03` — RB             | 30 (adapter)                 | 30 ✓             |
| `KU_3_month` — CaixaBank      | **140, chain complete**      | 132, chain 97%   |
| `KS_Bank_01` — RB             | **43 (adapter), reconciles** | 44 ✗             |
| `Statement_0093531803` — RB   | **44 (adapter), reconciles** | 45 ✗             |
| `US_Statement_03`             | refused                      | 6 of 8 ✗         |

The two failure modes are understood, not mysterious. Raiffeisenbank right-aligns the
original foreign-currency amount (`-509.00 UAH`) to the **same right edge** as the CZK
amount, so one movement beats twice — exactly the 509.00 discrepancy. And the US
statement prints deposits, withdrawals and daily balances as three tables with identical
columns, so "one rhythm per document" keeps only the largest.

### The consequence for the architecture

This is the outcome to build on, and it is better than a winner would have been. Both
assemblers become **candidate readings**, and the proof engine selects. In every case
where they disagree, arithmetic picks the right one without knowing which reader
produced it:

- CaixaBank — 140 completes the running chain, 132 reaches 97%. Chain proof wins.
- Raiffeisenbank January — 43 rows reconcile to the printed closing balance, 44 do not.
  Endpoint proof wins.

That is the governing rule doing exactly the job it was written for, on the one
experiment designed to break it. It only works once **`max(rowCount)` arbitration is
gone** (Phase C item 8) — under the current rule CaixaBank is fine but Raiffeisenbank
would silently pick the wrong reading, because 44 > 43.

Adding the rhythm assembler is therefore **Phase C item 13, and strictly after items 1–8**.
A second reader before the gate is universal is a second way to be confidently wrong.

### One production bug found by the prototype

`isDateLike` (`determinacy.ts:26`) has no range check on the components, so on any
dot-decimal statement every two-decimal money value parses as a date:

```
"88.40"  true    "120.00" true    "45.00"  true
"1.83"   true    "377.93" true    "1234.56" true
```

The comment directly above it claims the opposite ("anchored at both ends so a plain
number like `1.234` cannot be mistaken for a date") — true only for 3-decimal values.
In the prototype this made the entire UK "Paid out" column read as dates. Independent of
which assembler ships: **fix it regardless.** Related: a bare run of digits with no
decimal or group separator must not count as money — Raiffeisenbank's ten-digit
transaction reference sits one-per-record in its own column and is a _perfect_ amount
column by every structural test.

---

## The synthetic corpus — the first honest generalisation test

`scratch-workspace/synthetic-test/` (generated with Codex, gitignored): **486 files, 60
statements, 24 locales, 20 currencies, 16 archetypes**, each statement emitted in up to
10 formats. Ground truth gives exact per-row minor amounts, balances, fees and FX
originals. Its arithmetic is sound — 57 of 60 reconcile on endpoints _and_ carry a
complete running chain; the 3 exceptions are dormant-account statements that correctly
print no balances.

Its archetypes map onto our failure taxonomy deliberately: `rhythm-multiline`,
`foreign-amount`, `competing-tables`, `multi-sheet-workbook`, `concatenated-statements`,
`suffix-direction`, `split-direction`, `reverse-chronological`, `headerless-ledger`,
`metadata-csv`. It also carries the minor-unit cases our real corpus lacks entirely:
**KWD (three decimals)** and **JPY (zero)**.

Baseline of the current reader, scored on the ledger-meaningful quantity
(`amount − fee`), not raw `amountMinor` — the ground truth stores fees separately and we
fold them in, which is a representation difference, not an error:

```
FORMAT      files  read  refused  rows-exact  ccy-ok  amounts-exact
csv          120    60       60           3       0              3
tsv           60     0       60           0       0              0
pdf-text      57     0       57           0       0              0
camt053       19    19        0          19      19             19
mt940         15    15        0          15      15             15
abo            3     3        0           3       3              3
ofx           10     0       10           0       0              0
qif            8     0        8           0       0              0
xlsx           1     1        0           0       0              0
ods            1     0        1           0       0              0
```

**The three standards parsers are perfect: 37/37 files, every row, every amount, every
currency**, across all 24 locales including KWD's three decimals. That is the strongest
evidence in the release, and it is evidence for the _format-first_ thesis: where a
document states its own structure, no heuristics are needed and nothing goes wrong.

**Everything discovered rather than declared fails.** Real-corpus performance (11 of 19)
did not generalise, because the real corpus is what the reader was developed against.
This is the number to manage the release by.

Four distinct causes, none of them "PDFs are hard":

1. **Adapter hijack — 60 files.** The synthetic CSVs use mBank's `#`-prefixed header
   convention, so the mBank sniffer claims every comma-delimited file and then dies with
   `mBank: transaction header not found`. An adapter that cannot parse a file it claimed
   must **fall through to the generic reader**, never abort the import. This is the
   adapter routing being treated as a decision instead of a hypothesis — the governing
   rule's exact shape, one layer up.
2. **Silent zero-row reads — 57 files.** Every semicolon/win1250 CSV returns a statement
   with **zero rows and no complaint**. `got − want` equals `−rowcount` exactly. This is
   the RB January bug, reproduced 57 times. Production's `ingest.ts:517` catches it
   before the content hash is recorded, so these are refusals and not corrupt imports —
   but the engine must not report success on an empty read.
3. **Currency read from a metadata label — 60 files.** The generic reader returns
   `#Numer rachunku` — a Polish column heading for "account number" — as the ISO currency
   code. Worse than the `'CZK'` default, and further evidence for taking currency from
   the account (see above) rather than from the document.
4. **PDF fragmentation — 57 files.** Every synthetic PDF is refused, and the message is
   specific: `3 of 8 rows`, `2 of 5 rows`. The reader finds the table and recovers a
   third of it. This is precisely what the rhythm assembler fixes, and it refuses rather
   than importing a third of a statement — the proof gate doing its job.

Unsupported by design and correctly refused: OFX (10), QIF (8), ODS (1).

Two smaller findings: 4 files are refused for an undecidable decimal separator (`every
comma is…`, `every dot is…`), which is the determinacy engine working as intended; and
TSV is read as a grid but yields no evidence on 55 of 60, suggesting tab is not reaching
the delimiter candidates.

**This corpus should become the acceptance suite** once Phase C lands — it is larger than
the real corpus by a factor of twenty-five, it is safe to commit (no real financial
data), and unlike `bank_data_examples_do_not_share/` it can live in CI.

---

## Open risks

- **The safety boundary trusts attacker-supplied numbers.** Highest-severity open item
  after the wrong-currency import.
- **Unanchored P3 cannot see a movement dropped from the head of the file** — the
  Revolut and CaixaBank case, 178 of 432 imported rows.
- **A workbook silently drops every sheet but one**, and records the content hash, so
  the corrected re-import is refused as a duplicate.
- **Tesseract loses summary blocks** while reading transaction tables well, so stated
  totals may be unavailable on scans even where the format prints them. The chain is the
  dependable oracle there — which makes anchoring it (Phase C item 7) more important, not
  less.
- **Image quality floor** — no rendered page has yet reconciled at any quality. The floor
  is not "300 dpi"; it is currently unknown, and must be measured before the image
  formats stay in the upload dialog.
- **Unreachable code that looks finished.** OCR and the profile store both read as
  delivered features in a code review and are neither. This is how the previous draft of
  this document came to be a third wrong.
